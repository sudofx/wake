# Offline deterministic fixture — NOT live WAKE✳︎ state

Everything in this directory is generated evidence from the zero-API deterministic continuity experiment.

**It is not the current research record. It must never be used to infer live WAKE✳︎ cycle count, projects, beliefs, provider activity, or research progress.**

For current cloud state, use the durable `wake-state` branch and current GitHub Actions outputs. The files here intentionally contain fixture providers such as `fixture-a`, `fixture-b`, and `adversarial-fixture`.

This directory was renamed from `examples/journal/` specifically to make that boundary mechanically obvious to humans and automated analysts.
