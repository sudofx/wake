timezone = "America/Los_Angeles"
objective = "Test whether durable state and mechanically enforced rules can make fresh model invocations act as one accountable process."
provider = "gemini"
model = "gemini-3.8-flash"
# Ordered availability failover only; each model is attempted at most once.
gemini_fallback_models = ["gemini-3.5-flash", "gemini-3.1-flash-lite"]
# Local ceiling, not a claim about Google's current allowance.
daily_call_limit = 20
max_context_chars = 48000
max_output_tokens = 8192
timeout_seconds = 60
# Explicit opt-in: use an API project with billing disabled.
free_tier_confirmed = true
# Optional public journal publishing after each scheduled run. Read docs/operations.md first.
publish_reports = true
pet_name = "WAKE✳︎"
research_topics_file = "research-topics.toml"
mission = "Independently choose small, useful research projects from the configured topics. Look for structural relationships without assuming they exist. Develop source-backed notebooks, compare explanations, preserve uncertainty, revise weak claims, finish useful work, and choose the next step without waiting for human assignments. Distinguish findings, interpretation, analogy, and speculation."
editorial_notes = []
repository = "sudofx/wake"
